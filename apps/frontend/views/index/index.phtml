<?php
/**
 * @var $blog_keyword;
 * @var \Learncom\Models\LearnArticle[] $blogs
 *  @var \Learncom\Models\LearnMenu[] $menus
 */
?>
<main style="background-color: #fafafa">
    <section id="hero">
        <img src="./././frontend/imagesbanner-he.jpg" alt="Banner">
    </section>
    <section class="section container">
        <div class="section-title" data-aos="fade-up">
            <p>Khai Giảng</p>
            <h2>Lịch Khai Giảng Mới Nhất</h2>
        </div>
        <section id="course-details" data-aos="fade-up">
            <div class="container">
                <div class="ui_kit-table">
                <table class="table table-custom">
                    <thead class="thead-light">
                        <tr>
                            <th scope="col"  class="font-weight-bold">Khóa học</th>
                            <th scope="col" class="font-weight-bold">Lớp học</th>
                            <th scope="col" class="font-weight-bold">Lịch học</th>
                            <th scope="col" class="font-weight-bold" style="width: 150px">Khai giảng</th>
                        </tr>
                    </thead>
                    <tbody id="offline-loader">
                        <?php foreach($blogs as $blog) :
                            ?>
                        <tr>
                            <td rowspan="1"><a href="<?= $this->url->get("/blogs/" . $blog['article_keyword']) ?>"
                                    class="text-primary"><?= $blog['article_name'] ?></td>
                            <td><?= $blog['article_icon'] ?></td>
                            <td><?= $blog['article_content_show'] ?></td>
                            <td><?= $blog['article_time'] ?></td>
                        </tr>
                        <?php endforeach ?>
                       
                    </tbody>
                </table>
                </div>
            </div>
        </section>
        </div>
    </section>
    <section class="counter">
        <div class="container">
            <div class="row counters" data-aos="zoom-in">
                <div class="col-lg-3 col-6 text-center">
                    <span data-purecounter-start="0" data-purecounter-end="<?= $total_user ?>"
                        data-purecounter-duration="1" class="purecounter"></span>
                    <p>Học sinh</p>
                </div>
                <div class="col-lg-3 col-6 text-center">
                    <span data-purecounter-start="0" data-purecounter-end="<?= $total_video ?>"
                        data-purecounter-duration="1" class="purecounter"></span>
                    <p>Bài Giảng</p>
                </div>

                <div class="col-lg-3 col-6 text-center">
                    <span data-purecounter-start="0" data-purecounter-end="<?= $total_document ?>"
                        data-purecounter-duration="1" class="purecounter"></span>
                    <p>Tài Liệu</p>
                </div>

                <div class="col-lg-3 col-6 text-center">
                    <span data-purecounter-start="0" data-purecounter-end="3" data-purecounter-duration="1"
                        class="purecounter"></span>
                    <p>Giáo viên</p>
                </div>
            </div>
        </div>
    </section>
    <?php
    foreach ($arrClassId as $class_id):
        $class_name = "";

        foreach ($arrClass as $class) {
            if ($class['class_id'] == $class_id) {
                $class_name = $class['class_name'];
            }
        }
        ?>
        <section class="section container">
            <div class="section-title" data-aos="fade-up">
                <p>Các Bài Giảng Mới Nhất
                </p>
                <h3>Bài giảng Môn Toán
                    <?= $class_name ?></h3>
            </div>
            <div class="row section-slick" data-aos="zoom-in" data-aos-delay="100">
                <?php
                $total_video = 0;
                foreach ($videos as $key => $video):
                    if ($video['video_class_id'] != $class_id) {
                        continue;
                    }
                    $total_video++;
                    if ($total_video > 4) {
                        break;
                    }
                    ?>
                    <div class="col-lg-3 col-md-6 d-flex align-items-stretch mt-4 mt-md-0">
                        <div class="course-item" href="<?= $this->url->get("/khoahoc?classId=21&subjectId=5") ?>">
                            <div class="course-img">
                                <embed src="<?= $video['video_link'] ?>" type="application/pdf" height="100%" width="100%">
                            </div>
                            <div class="course-content">
                                <div class="d-flex justify-content-between align-items-center mb-3">
                                    <h4>
                                        <?= $class_name ?>
                                    </h4>
                                </div>
                                <h3>
                                    <a href="<?= $this->url->get("/tailieu?classId=21&subjectId=5") ?>"><?= $video['video_name'] ?></a>
                                </h3>
                                <p>
                                    <?= $video['video_name'] ?>
                                </p>
                                <div>
                                    <div class="trainer-rank d-flex align-items-center">
                                        <i class='bx bxs-calendar'></i>
                                        <span><?= date("d/m/Y",strtotime($video['video_insert_time'])) ?></span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                <?php endforeach ?>
            </div>
        </section>
    <?php endforeach ?>

</main>